/* 논리 자료형(참 or 거짓) */
let a = true;
let a1 = false;
//let a="true"; //문자 => 숫자로 바꾸면 NaN
alert(a);

/* 자료형을 숫자로 바꿔주는 함수 Number() */
document.write(`${Number(a)} <br>`); //true = 1
document.write(Number(a1)); //false = 0
